services:
  pdf-service:
    build:
      context: ..
      dockerfile: pdf-service/Dockerfile
    environment:
      PORT: "8080"
      INTERNAL_API_KEY: ${PDF_INTERNAL_API_KEY}
    expose:
      - "8080"
